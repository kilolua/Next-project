import { FC } from 'react'

import Layout from '@/components/layout/Layout'

import { IHome } from '@/screens/home.interface'

const Home: FC<IHome> = () => {
	return <Layout>Home page</Layout>
}

export default Home
